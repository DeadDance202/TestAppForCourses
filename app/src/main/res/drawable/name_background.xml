<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/primary" /> <!-- Основной цвет фона -->
            <corners android:radius="8dp" /> <!-- Закругленные углы -->
            <stroke
                android:width="2dp"
                android:color="@color/white" /> <!-- Обводка (при необходимости) -->
        </shape>
    </item>
</selector>